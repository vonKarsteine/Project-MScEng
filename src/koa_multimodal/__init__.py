"""Multimodal Kellgren-Lawrence knee osteoarthritis grading.

Deliberately exports nothing. Every ``__init__.py`` in this package is empty so
that an import line names the module it depends on, which is what makes the
layering rule in ``tests/test_import_layers.py`` decidable.
"""

__version__ = "7.0.0"
